language: 
    - python
python:
    - 3.4
    - 3.5
    - 2.7
before_install:
    - sudo apt-get -qq update
install: 
    - pip install -r requirements.txt
# script:
    # - python manage.py test
# after_success:
    # - coverage report