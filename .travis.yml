language:
    - python
python:
    - 3.8
    - 3.6
    - 2.7
env:
    - TRAVIS_BUILD_DIR=website/
before_install:
    - sudo apt-get -qq update
install: 
    - pip install -r requirements.txt
script:
    - tox
# after_success:
    # - coverage report